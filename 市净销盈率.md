**市净/销/盈率**

df = get-fundamentals(

    query(

        valuation.code,

        valuation.pb\_ratio,

        valuation.ps\_ratio

    ).filter(

        valuation.pb\_ratio < 1.5,净

        valuation.ps\_ratio < 0.5,销

        valuation.pcf\_ratio < 6,动

        valuation.pe\_ratio > 3,静

        valuation.pe\_ratio < 5

    ).order\_by(

        valuation.pe\_ratio.desc()  # 注意：des()改为desc()

    ),

    date='2022-1-6'

)



print(df\[:5])



***龙虎榜***

\# 获取最近有数据的日期（例如查询时是2024年1月，则用2024-01-16）

df = get-billboard\_list(stock\_list=None, end\_date='2026-01-16', count=2)

print(df\[\['code', 'abnormal-name', 'sales-depart-name', 'rank']]\[:20])



**净资产收益率/总资产净利率**

from jqdata import \*

import pandas as pd



\# 获取高ROE股票

df = get\_fundamentals(

    query(

        indicator.code,

        indicator.roe,

        indicator.roa

    ).filter(

        indicator.roe > 20,

        indicator.roa > 5

    ).order\_by(

        indicator.roa.desc()

    ),

    date='2026-01-16'

)



\# 检查是否有数据

if df.empty:

    print("未找到ROA>5%的股票")

else:

    # 获取股票名称

    # 方法：通过get\_all\_securities获取所有股票信息，然后映射名称

    all\_stocks = get\_all\_securities(\['stock'], date='2026-01-16')

 

    # 创建代码到名称的映射

    stock\_names = all\_stocks\['display\_name']

 

    # 为每只股票添加名称

    df\['name'] = df\['code'].map(stock\_names)

 

    # 格式化输出

    print("=" \* 50)

    print(f"查询日期: 2026-01-16 | ROA > 50% 的股票 (前{min(10, len(df))}只)")

    print("=" \* 50)

 

    # 显示前10条记录

    result = df\[\['code', 'name', 'roe','roa']].head(10)

 

    # 美化输出

    for i, (\_, row) in enumerate(result.iterrows()):

        code = row\['code']

        name = row\['name'] if pd.notna(row\['name']) else '未知'

        roe = f"{row\['roe']:.2f}%"

        roa = f"{row\['roa']:.2f}%"

        print(f"{i+1:2d}. {code:10} {name:10} ROE： {roe:>8} ROA: {roa:>8}")



**JS加速线**

**from jqlib.technical\_analysis import\***

**security = '002361.XSHE'**

**JAX,YLX,ZCX = GDX(security,check\_date = '2026-01-20',N=14,M=9)**

**for stock in security:**

    \*\*print(stock,"济安线：",JAX)\*\*

    \\\*\\\*print(stock,"压力线: ",YLX)\\\*\\\*

&#x20;   \\\\\\\*\\\\\\\*print(stock,"支撑线：",ZCX)\\\\\\\*\\\\\\\*










**GDX 压力支撑线**

**forfrom jqlib.technical\_analysis import\***

**security = '002361.XSHE'**

**\_JS,MA1,MA2,MA3 = JS(security,check\_date = '2026-01-20',N=5,M1=5,M2=10,M3=20)**

**print(\_JS)**

**print(MA1)**

**print(MA2)**

**print(MA3)**



**MA**

**from jqlib.technical\_analysis import\***

**security = '002361.XSHE'**

**check\_date = '2016-01-20'**

**MA5 = MA(security,check\_date=check\_date,timeperiod=5)**

**MA10 = MA(security,check\_date=check\_date,timeperiod=10)**

**MA20 = MA(security,check\_date=check\_date,timeperiod=20)**

**print(check\_date,"五日线",MA5\[security])**

**print(check\_date,"十日线",MA10\[security])**

**print(check\_date,"二十日线",MA20\[security])**



BOLL

 

